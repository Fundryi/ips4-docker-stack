# Database Configuration
MYSQL_PASSWORD=change_me_to_strong_password
MYSQL_ROOT_PASSWORD=change_me_to_strong_root_password

# HTTP Port Configuration
HTTP_PORT=80

# HTTPS Port Configuration
HTTPS_PORT=443

# SSL Configuration
# When using HTTPS, run: docker compose --profile https up -d
# When using HTTP only, run: docker compose up -d

# Your domain name (required for SSL certificate)
DOMAIN=example.com

# Email for Let's Encrypt notifications (certificate expiry warnings)
CERTBOT_EMAIL=admin@example.com
