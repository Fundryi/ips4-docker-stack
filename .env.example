# ============================================
# Docker Compose Profiles
# ============================================
# Set COMPOSE_PROFILES to activate optional services
# Empty = no proxy (HTTP only on HTTP_PORT)
# proxy = enable reverse proxy with SSL/HTTPS support
COMPOSE_PROFILES=

# ============================================
# Database Configuration
# ============================================
MYSQL_PASSWORD=change_me_to_strong_password
MYSQL_ROOT_PASSWORD=change_me_to_strong_root_password

# ============================================
# HTTP Port Configuration
# ============================================
# Default is 8080, change if needed
# When using reverse proxy, you can keep this as 8080 (internal)
HTTP_PORT=8080

# ============================================
# Reverse Proxy Configuration
# ============================================
# The proxy service will handle SSL/HTTPS and port forwarding

# Public ports for reverse proxy
PROXY_HTTP_PORT=80
PROXY_HTTPS_PORT=443
PROXY_UI_PORT=81

# Nginx Proxy Manager default credentials
# CHANGE THESE AFTER FIRST LOGIN!
PROXY_EMAIL=admin@example.com
PROXY_PASSWORD=changeme

# Cloudflare DNS Challenge (for wildcard certificates)
# Get API token from: Cloudflare Dashboard -> My Profile -> API Tokens
# Required permissions: Zone -> DNS -> Edit
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_EMAIL=
