# ============================================
# Database Configuration
# ============================================
MYSQL_PASSWORD=change_me_to_strong_password
MYSQL_ROOT_PASSWORD=change_me_to_strong_root_password

# ============================================
# Port Configuration
# ============================================
# HTTP Port - redirects to HTTPS
HTTP_PORT=80

# HTTPS Port - main site with SSL
HTTPS_PORT=443

# ============================================
# SSL Configuration
# ============================================
# SSL certificates should be placed in ./data/ssl/
# Files needed:
#   - fullchain.pem (certificate + intermediate chain)
#   - privkey.pem (private key)
#
# For Let's Encrypt, use certbot:
#   certbot certonly --webroot -w /var/www/html -d yourdomain.com
#   Copy certificates to ./data/ssl/
